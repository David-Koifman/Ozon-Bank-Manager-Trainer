### 🔍 Пояснение полей спецификации

- **`type: "binary"`** → 1 балл за соблюдение, 0 — за нарушение.  
- **`type: "scale"`** → 0–2 балла по рубрике.  
- **`must_contain`** → все фразы должны присутствовать.  
- **`must_contain_any`** → хотя бы одна из фраз.  
- **`must_not_contain`** → запрещённые слова (в `critical_errors`).  
- **`penalty_if_false`** → штраф в баллах (можно использовать для агрегации).  
- **`passing_threshold`** → проходной балл (меньше — требуется повтор).  
