# Сценарии для голосового тренажёра

Папка `scenarios/` содержит **единый источник истины** для поведения ИИ-клиента и оценки менеджера.  
Каждый сценарий описывает:

- цель тренировки,  
- профиль клиента (архетип, эмоции, типичные возражения),  
- этапы диалога,  
- compliance-требования (обязательные и запрещённые фразы),  
- критерии оценки,  
- параметры поведения (архетипы, уровни сложности).

Сценарии используются **одновременно**:
- **ИИ-клиентом** — для генерации реалистичных реплик,  
- **LLM-judge** — для оценки транскрипции звонка.

---

## 📄 Формат сценария

Каждый файл — валидный JSON с обязательными разделами:

| Раздел | Назначение |
|-------|-----------|
| `scenario_id` | Уникальный идентификатор (для привязки промптов и аналитики) |
| `client_profile` | Портрет клиента: статус, ситуация, черты характера |
| `dialog_objectives` | Цель тренировки и второстепенные задачи |
| `compliance_requirements` | Обязательные и запрещённые фразы (для оценки) |
| `aida_flow` | Этапы диалога: внимание → интерес → желание → действие |
| `client_behavior_presets` | Архетипы (`novice`, `aggressor`...) и уровни сложности (1–4) |
| `evaluation_criteria` | Критерии оценки и проходной балл |

---

## 🔁 Связь с другими компонентами

- **Промпты**: находятся в `/src/prompts/dialog_agent/` с тем же именем (например, `ozon_rko_new_seller.md`).  
- **Датасет**: генерируется скриптом `generate_dataset.py` на основе этих JSON.  
- **Оценка**: LLM-judge использует `evaluation_criteria` для анализа транскрипции.

---

## ✏️ Как добавить новый сценарий

1. Создайте файл в подходящей подпапке:  
   `scenarios/b2b/название_продукта.json`
2. Используйте [ozon_rko_new_seller.json](b2b/ozon_rko_new_seller.json) как шаблон.
3. Добавьте соответствующий промпт в `/src/prompts/dialog_agent/`.
4. Убедитесь, что `scenario_id` уникален и соответствует имени файла.

> 💡 Все изменения в сценариях **автоматически применяются** к тренировкам и оценке — перезапуск кода не требуется.

---

## 📌 Важно

- **Не дублируйте сценарии** для разных архетипов — используйте `client_behavior_presets`.  
- **Не включайте в JSON текстовые «идеальные диалоги»** — они генерируются динамически.  
- **Все сценарии должны проходить валидацию** (проверка структуры) перед использованием.
